// This component is deprecated and no longer used in the new UI design.
// The desktop is now clean, and apps are launched via the new AppLauncher.
import React from 'react';

const DesktopIcon: React.FC = () => {
  return null;
};

export default DesktopIcon;